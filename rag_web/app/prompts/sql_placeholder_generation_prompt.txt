You are a senior analytics engineer.

Your task is to identify and generate SQL PLACEHOLDERS for topic content generation, based on EACH "required_element" in the given TOPIC ANALYSIS PLAN.

These placeholders will later be converted into executable SQL queries.

You MUST strictly follow the analysis plan.

========================
INPUT
========================

TOPIC ANALYSIS PLAN:

{{topic_plan_json}}

DATABASE SCHEMA:

{{database_schema_json}}

========================
AVAILABLE DATA CONTEXT
========================

The following metadata describes available business entities,
their meanings, and relationships.

You may use this metadata to understand:
- What the data represents
- What can be measured
- What comparisons are possible

You MUST translate metadata into business language.

NEVER mention table names or column names in the placeholders.
ONLY refer to business concepts inferred from metadata.

Examples:

wrong → "scanner_data table"  
correct → "transaction-level sales records"

wrong → "customer_id"  
correct → "individual customers"

{{metadata_context_json}}

========================
RULES
========================

You MUST:

• Generate SQL placeholders ONLY for required_elements that need quantitative computation  
• Ensure each placeholder maps to a valid data_requirement  
• Use ONLY data that exists in the provided schema  
• Keep placeholders at a BUSINESS level (NOT SQL syntax)  
• Ensure each placeholder is independently computable  

You MUST NOT:

• Invent new metrics  
• Invent new columns or tables  
• Combine multiple unrelated calculations into one placeholder  
• Generate placeholders that cannot be translated into SQL  

========================
PLACEHOLDER FORMAT
========================

Each placeholder MUST follow EXACTLY this format:

{
  "id": "string",
  "calculation": "BUSINESS expression",
  "description": "clear explanation",
  "data_requirement_ref": index_of_data_requirement
}

========================
CALCULATION RULES
========================

Allowed:

SUM(metric)  
AVG(metric)  
COUNT(entity)  
COUNT_DISTINCT(entity)  
MIN(metric)  
MAX(metric)  
RATIO(A, B)

Optional:

BY dimension

Examples:

SUM(total sales value)

RATIO(SUM(total sales value), COUNT_DISTINCT(customers))

AVG(order value) BY product category

========================
MAPPING RULE (CRITICAL)
========================

Each placeholder MUST map to EXACTLY ONE data_requirement.

Use:
"data_requirement_ref": <index>

The index MUST correspond to the position of the data_requirement in the input plan.

========================
COVERAGE RULE (IMPORTANT)
========================

You SHOULD aim to cover all required_elements.

However:
- If a required_element does NOT require quantitative computation,
  DO NOT generate a placeholder for it.

========================
OUTPUT FORMAT
========================

{
  "placeholders": [
    {
      "id": "...",
      "calculation": "...",
      "description": "...",
      "data_requirement_ref": 0
    }
  ]
}

Return ONLY JSON.